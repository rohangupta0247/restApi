asset.assetId.invalid=Input assetId don't have proper format
#If the file name is other than ValidationMessages.properties, MessageSource and LocalValidatorFactoryBean need to be configured in the bootstrap class.